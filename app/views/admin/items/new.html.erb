<h1>管理者＃商品登録</h1>

<%= form_with model: [:admin, @item] do |item| %>
	<%= item.label :image, '商品画像' %>
	<%= item.attachment_field :image %><br>
	<%= item.label :name, '商品名' %>
	<%= item.text_field :name %><br>
	<%= item.label :introduction, '説明文' %>
	<%= item.text_area :introduction %><br>
	<%= item.label :genre_id, 'ジャンル名' %>
	<%= item.collection_select :genre_id, @genres, :id, :name, include_blank: "選択して下さい" %><br>
     <!-- :id は選択肢にその場で振られる番号。nameはデータベースのテーブル（今回はgenresテーブル）どのカラムを選択肢名として表示するかを選択。
	 genre_idは実際にデータを入れるカラム。@genresはどのデータを参照するか。　 -->
	<%= item.label :price, '値段' %>
	<%= item.text_field :price %><br>
	<%= item.label :is_active, '販売ステータス' %>
	<%= item.radio_button :is_active, "true", :checked => true %> <!-- radio_buttonにcheckedをつける -->
	<%= item.label "販売中", {value: :販売中, style: "display: inline-block;"} %>
	<%= item.radio_button :is_active, "false" %> <!-- :is_active部分を異なる名前にすると複数選択ができてしまう。 -->
	<%= item.label "売切れ", {value: :売切れ, style: "display: inline-block;"} %><br>
	<%= item.submit '登録' %>
<% end %>

