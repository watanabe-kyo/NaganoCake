<h1>エンドユーザー＃退会ページ</h1>

<% if end_user_signed_in? && @end_user.id == current_end_user.id %>
	<p><b>本当に退会しますか？</b></p>
	<%= link_to "退会しない", root_path(current_end_user), class: "btn btn-info" %>
	<%= link_to "退会する", end_user_path(current_end_user), method: :delete, class: "btn btn-danger" %>
<% end %>
